@model IEnumerable<SistemaGestionTareas.Usuario>

@{
    ViewData["Title"] = "Index";
}

<h1 class="mb-4">Usuarios</h1>
<div class="container mt-4">
    <p>
        <a asp-action="Create" class="btn btn-primary">Create New</a>
    </p>
    <table class="table table-hover table-bordered align-middle">
        <thead class="table-dark">
            <tr>
                <th>@Html.DisplayNameFor(model => model.Id)</th>
                <th>@Html.DisplayNameFor(model => model.Nombre)</th>
                <th>@Html.DisplayNameFor(model => model.Contraseña)</th>
                <th>@Html.DisplayNameFor(model => model.CorreoElectronico)</th>
                <th>@Html.DisplayNameFor(model => model.Rol)</th>
                <th>Acciones</th>
            </tr>
        </thead>
        <tbody>
            @foreach (var item in Model)
            {
                <tr>
                    <td>@Html.DisplayFor(modelItem => item.Id)</td>
                    <td>@Html.DisplayFor(modelItem => item.Nombre)</td>
                    <td>@Html.DisplayFor(modelItem => item.Contraseña)</td>
                    <td>@Html.DisplayFor(modelItem => item.CorreoElectronico)</td>
                    <td>@Html.DisplayFor(modelItem => item.Rol)</td>
                    <td>
                        <a href="@Url.Action("Edit", "Usuarios", new { id = item.Id })" class="btn btn-sm btn-warning">Edit</a> |
                        <a href="@Url.Action("Details", "Usuarios", new { id = item.Id })" class="btn btn-sm btn-info">Details</a> |
                        <a href="@Url.Action("Delete", "Usuarios", new { id = item.Id })" class="btn btn-sm btn-danger">Delete</a>
                    </td>
                </tr>
            }
        </tbody>
    </table>
</div>
